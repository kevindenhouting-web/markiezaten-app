{
  "entities": {
    "Player": {
      "title": "Player",
      "description": "A player in the team",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "The name of the player"
        },
        "season": {
          "type": "string",
          "description": "The season this player belongs to"
        }
      },
      "required": ["name", "season"]
    },
    "Match": {
      "title": "Match",
      "description": "A football match",
      "type": "object",
      "properties": {
        "date": {
          "type": "string",
          "format": "date-time",
          "description": "The date and time of the match"
        },
        "opponent": {
          "type": "string",
          "description": "The name of the opponent"
        },
        "isHome": {
          "type": "boolean",
          "description": "Whether it is a home match"
        },
        "gatheringTime": {
          "type": "string",
          "description": "The time the team gathers"
        },
        "attendance": {
          "type": "object",
          "description": "Map of playerId to boolean presence"
        },
        "formation": {
          "type": "string",
          "enum": ["4-4-2", "4-3-3"],
          "description": "The tactical formation"
        },
        "lineup": {
          "type": "object",
          "description": "Map of positionKey to playerId"
        },
        "score": {
          "type": "object",
          "properties": {
            "home": { "type": "integer" },
            "away": { "type": "integer" }
          },
          "description": "The final score of the match"
        },
        "scorers": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Array of player IDs who scored goals"
        },
        "season": {
          "type": "string",
          "description": "The season this match belongs to"
        }
      },
      "required": ["date", "opponent", "formation", "season"]
    },
    "Season": {
      "title": "Season",
      "description": "A football season name",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "The season name e.g. 28/29"
        },
        "createdAt": {
          "type": "string",
          "description": "ISO timestamp when created"
        }
      },
      "required": ["name"]
    }
  },
  "firestore": {
    "/players/{playerId}": {
      "schema": "Player",
      "description": "Collection of players"
    },
    "/matches/{matchId}": {
      "schema": "Match",
      "description": "Collection of matches"
    },
    "/seasons/{seasonId}": {
      "schema": "Season",
      "description": "Collection of custom seasons"
    }
  }
}
